{% extends 'base.html.twig' %}

{% block title %}Hello RestaurantController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>{{ restaurant.name }}</h1>

    <ul>
        {% for productRestaurant in restaurant.productRestaurants %}
            <li>
                {{ productRestaurant.product.name }} - {{ productRestaurant.price }}€
                {% if productRestaurant.stock < 60 %}
                    <span style="color: red;">(Stock faible!)</span>
                {% endif %}
            </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
